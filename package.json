{
  "name": "yard",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "bun run --filter '*' typecheck",
    "dev": "bun run --filter '*' --elide-lines 0 dev",
    "dev:coordinator": "bun run --filter '@yard/coordinator' dev",
    "dev:web": "bun run --filter '@yard/web' dev",
    "db:generate": "bun run --filter '@yard/db' generate",
    "db:migrate": "bun run --filter '@yard/db' migrate",
    "db:studio": "bun run --filter '@yard/db' studio",
    "build": "bun run --filter '*' build",
    "protocol:gen": "bun packages/protocol/scripts/gen-rust.ts",
    "protocol:check": "bun test packages/protocol && bun run protocol:gen && git diff --exit-code -- packages/provider/crates/yard-protocol",
    "test": "bun test packages/protocol packages/coordinator/test packages/web/test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
