# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
build/
out/
target/
*.tsbuildinfo

# env
.env
.env.*
!.env.example

# The provider's real configuration: device serials and host addresses are
# specific to one machine. provider.example.yaml is the one that ships.
packages/provider/provider.yaml
packages/provider/provider.local.yaml

# logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# editors / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# runtime scratch
/scratch/
/data/
*.pem
*.key
!**/testdata/*.key

# rust
/target/

# flutter
.dart_tool/
.flutter-plugins-dependencies

# NOTE: packages/provider/crates/yard-protocol/src/generated.rs and
# tests/fixtures.json are generated but deliberately COMMITTED — CI regenerates
# and diffs them to catch wire-contract drift. Do not add them here.

# local-only docs: not relevant outside this machine, never committed
*.local.md
