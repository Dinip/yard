# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
build/
out/
target/
*.tsbuildinfo

# env
.env
.env.*
!.env.example

# The provider's real configuration: device serials and host addresses are
# specific to one machine. provider.example.yaml is the one that ships.
packages/provider/provider.yaml

# logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# editors / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# runtime scratch
/scratch/
/data/
*.pem
*.key
!**/testdata/*.key

# reference sources kept on disk outside this repo:
#   ../stf ../stf-ios-provider ../idevice
# they are intentionally NOT vendored here. See docs/REFERENCES.md

# rust
/target/

# NOTE: packages/provider/crates/farm-protocol/src/generated.rs and
# tests/fixtures.json are generated but deliberately COMMITTED — CI regenerates
# and diffs them to catch wire-contract drift. Do not add them here.
