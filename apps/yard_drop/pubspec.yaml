name: yard_drop
description: 'YARD Drop: the YARD - Device Farm share companion.'
publish_to: none
version: 0.4.2
environment:
  sdk: ^3.9.2
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
